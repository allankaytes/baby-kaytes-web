<%- include("./partials/header") %>
    <div class="container">
        <% if(currentUser && currentUser.isAdmin) { %>
            <h1>You're an admin!</h1>
        <% } %>
        <header class="jumbotron">
            <div class="container">
                <h1><span class="glyphicon glyphicon-tent"></span> Welcome to Zuri's Baby Shower!</h1>
                <p>
                   <a class="btn btn-primary btn-lg" href="/">Registry</a>
                </p>
                 <p>
                    <a class="btn btn-primary btn-lg" href="/">Rsvp</a>
                </p>
            </div>
        </header>
<%- include("./partials/footer") %>